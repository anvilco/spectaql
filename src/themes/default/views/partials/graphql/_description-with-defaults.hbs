{{ md (
    concat
      (interpolateReferences description)
      (ternary defaultValue
        (concat "Default = "  (codify defaultValue))
        ""
      )
    joiner=". " filterFalsy=true
  ) stripParagraph=true
}}