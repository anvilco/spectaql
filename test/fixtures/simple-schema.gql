"A type."
type MyType {
  "A field on a type."
  myField(
    "An argument on a field on a type."
    myArg: String
  ): String
}

"The query object."
type Query {
  "A query."
  myQuery(
    "An argument to a query."
    myArg: String
  ): String
}

"The mutation object."
type Mutation {
  "A mutation."
  myMutation(
    "An argument to a mutation."
    myArg: String
  ): String
}
